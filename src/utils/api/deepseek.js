// src/utils/api/deepseek.js
import { API_URLS } from '../constants';

export const fetchDeepSeekResponse = async (message) => {
  // Implementación real con fetch/axios
  return {
    success: true,
    response: "Respuesta simulada de DeepSeek"
  };
};